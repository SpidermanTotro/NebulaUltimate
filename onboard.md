# Nebula Ultimate: Onboarding

Welcome! You can add or remove AI agents, code plugins and docs with:
- Drop new `.py` files in `/ai_agents` (AI skills)
- Drop code/plugin modules in `/plugins`
- Add markdown and gallery files in `/docs`, `/assets`

Launch with a running local LLM (see README).