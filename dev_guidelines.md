# Developer Guidelines
- All new features/modules must include tests in `/tests`.
- Markdown docs for all major updates.
- AI skills must document what LLM/version/prompt used.
- Follow code style checked by Black (Python) or Prettier (JS/TS).
...